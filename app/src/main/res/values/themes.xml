<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AuraLife" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@color/white_dark</item>
    </style>
    <color name="color_button">@color/white</color>
    <color name="color_icon">@color/black</color>
    <style name="Theme.AuraLife" parent="Base.Theme.AuraLife" />
</resources>